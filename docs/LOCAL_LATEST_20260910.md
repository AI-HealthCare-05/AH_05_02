# 최신 로컬 통합본 · 2026-09-10

## 실행 주소

- 최신 프론트 소개: http://127.0.0.1:8023/
- 로그인: http://127.0.0.1:8023/?auth=login
- 최신 당근의 숲 개인 시연: http://127.0.0.1:8023/forest?demo=1
- 상태: http://127.0.0.1:8023/api/health

8023은 별도 SQLite DB를 사용하는 **화면·게임 시연 환경**입니다. 실제 모델 추론 환경이 아닙니다. 기존 8022 프로세스, MySQL 데이터, 작업 폴더를 그대로 보존했습니다. 8022에서 가입한 계정이 이 별도 DB로 자동 복사되지는 않습니다.

## 가져온 코드

로컬 브랜치: `codex/local-latest-20260910` (GitHub에 푸시하지 않음).

- #43 `ab27587`: API·Worker·모델·게임 통합 기준.
- #35 `dcd5563`: 최신 숲 게임. #43에 이미 포함되어 있음을 ancestry로 확인.
- #44 `777b65e`: Apple 정제, RAG·퀴즈, 프로필·숲 이름 연동 수정.
- #42 `68dd9f6`: 사용자 확정 프론트 및 방·숲 배경.

백엔드의 새 기능을 작성하지 않았습니다. 병합 시 프로필 라우트·숲 이름 동기화는 #44 파일을 선택하고, 대시보드의 중복 응답 키는 #43 파일을 유지했습니다. 설정의 중복 주석을 정리했습니다. #44에 섞인 오래된 프론트·게임 변경은 최신 #43/#42 내용으로 유지했습니다.

## 모델 상태: 원본 전달 대기

GitHub 릴리스와 조회한 브랜치에는 등록된 가중치 바이너리가 없습니다. 로컬 Downloads의 여섯 joblib도 아래 두 체크섬과 일치하지 않았습니다. 사용자가 원본 위치를 다음 날 확인하기로 했습니다.

| 대상 | 필요한 파일 SHA-256 | 상태 |
|---|---|---|
| 현재 선별 v0.6.1 | `ffc6743849973676308703dd6bd5af0f8d557d5f45a8886e6660f86c81b85178` | 원본 없음 |
| 미래 위험 RF25 v1.1 SAV | `b96eaf408982399782073fce97977bef874012cf7d90551120da60266df68ddd` | 원본 없음 |

기존 shared7·첫 구간 앙상블·RF25 v1 파일은 원래 위치에 보존했습니다. 최신 모델 대신 쓰거나 체크섬을 바꿔 통과시키지 않았습니다.

원본을 받으면 이 저장소의 `scripts/provision-models.py --today <원본> --tomorrow <원본>`로 체크섬 검증 후 설치합니다. v0.6.1은 upstream의 별도 `ai_worker/Dockerfile.current-screening` 환경을 사용해야 합니다. 실제 Worker 기동·DB 연결·예측 원사이클 검증은 미완료입니다.

## 로컬 재실행

이 저장소 루트에서 기존에 설치된 Python 환경을 재사용합니다.

```bash
../AH_05_02-s2-api002/.venv/bin/python scripts/run_latest_preview.py
```

기본 포트는 8023입니다. `--port`로 다른 포트를 지정할 수 있습니다. 원래 8022 서비스가 실행 중인 동안 8022를 지정하지 마세요.

## 게임 이미지

upstream `scripts/import_forest_kittens.ps1`, `scripts/import_forest_rabbits.ps1`의 공식 무료 다운로드 절차와 정확한 PNG 이름·규격을 사용했습니다. macOS에서는 임시 Python 실행본으로 같은 검증·설치를 수행했습니다.

- 고양이 원본·무료 액세서리 18개, 토끼 2개 설치.
- 유료 팩 구매 없음. 원본 이미지는 기존 `.gitignore` 대상에만 저장하며 Git에 올리지 않음.
- 설치 폴더: `src/frontend/assets/animals/licensed-kittens`, `licensed-rabbits`.

## 검증과 남은 점검

- 게임·메인 JavaScript 문법 검사 통과.
- 프로필 이름, 웰니스, Apple 정제, 퀴즈, Worker 핸들러, 숲 API 대상 Python 검사: 101개 통과. 이는 실제 가중치 추론이나 실환경 DB 검증을 뜻하지 않음.
- 숲·챌린지 설정 Node 검사: 360 통과, 4 실패, 3 건너뜀. 실패 항목은 설정 버튼 문구, `returnToForestSettings` 기대, 오리의 연못 상호작용 기대, 새 메서드 추가 이후 실행 코드 추출 구간입니다. 실행 코드 문법 오류는 없으며 팀 코드·테스트 차이로 남겨 두었습니다.
- 브라우저: 게임 준비 완료, Phaser 활성화, 이동·달리기 입력, 로그인 화면 표시 확인.
- 게임 UI를 사용하면서 생기는 시연 기록은 8023의 독립 환경에만 저장됩니다.

전체 테스트 통과 또는 운영 배포 완료로 간주하지 않습니다. 최신 모델 원본을 받은 뒤 실제 API·Worker와 기존 계정 연결 검증이 다음 단계입니다.
