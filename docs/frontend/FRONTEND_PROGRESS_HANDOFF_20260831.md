# 프론트엔드 진행·인계 — 2026-08-31

## 오늘 확인·구현한 내용

- PR #23이 `develop` 대상 Draft이며 기존 Head CI가 통과한 상태임을 확인
- PR #19의 회원가입·프로필·예측 상태 계약 대조
- 네트워크 오류와 HTTP 오류 분리
- `401`, `409`, `422`, `500`, `503`, `504` 기본 오류 코드 분류
- 작업 시간초과를 별도 상태가 아닌 `failed + TIMEOUT`으로 처리
- 예측 작업 상태를 `queued/running/succeeded/failed` 네 값으로 통일
- 회원가입·로그인 API 장애 시 로컬 데모로 자동 전환하는 처리 제거
- 챌린지 API 장애 시 기본 예시를 정상 데이터처럼 표시하는 처리 제거
- 챌린지 오류·빈 목록 안내 추가
- 로컬 데모 토큰은 로컬 호스트에서만 유효하도록 제한
- 주간 리포트의 고정 날짜·요일 체크·예시 달성률 제거
- 주간 리포트를 실제 `challenge_details` 응답으로 표시
- 4주 전체·전체 기간 API 연결 전에는 예시 수치 대신 준비 상태 표시
- 건강교육 로딩·빈 데이터·오류 상태와 완료 버튼 중복 요청 방지 추가
- 관련 정적 테스트 추가

## PR #23에 바로 반영 가능한 변경

- API 오류 자동 데모 전환 제거
- 네트워크·서버·검증·충돌·모델 준비·시간초과 오류 구분
- 예측 상태값 네 종류 통일
- 챌린지 오류·빈 화면 처리
- 관련 테스트와 API 연결 상태 문서

## 팀 결정 후 반영할 변경

- 이용약관·개인정보·건강정보 동의의 최종 문구 및 버전 저장 방식
- 25개 모델 입력에 따른 건강정보 폼 확장
- 사용자에게 표시할 모델 버전·위험 범주·위험요인
- 요일별 기록·4주 집계·전체 회차 이력 API 계약
- 4주 건강교육의 주차별 공개 규칙

## PR #21 소속으로 분리할 변경

- 당근의 숲 백엔드·마이그레이션·게임 화면·게임 자산
- `/forest` 연결과 아바타·숲 꾸미기 기능
- PR #21 파일은 PR #23 계약 정비 커밋에 포함하지 않는다.

## 검증 결과

- `node --check src/frontend/app.js`
- `python -m pytest -q tests/test_prototype.py`
- `git diff --check`

## 남은 프론트 작업

- PR #19 기준 브랜치에서 가입→프로필→적합성 통합 테스트
- 백엔드 정상·오류 예시 수령 후 Mock과 실제 API 결과 대조
- 최종 모델 입력 계약 수령 후 폼·단위·범위 재검토
- 요일별 기록·4주 집계·전체 회차 이력 API 수령 후 리포트 확장
- PR #23용 변경만 선별하여 커밋·푸시
- 푸시 후 새 CI와 리뷰 결과 확인
