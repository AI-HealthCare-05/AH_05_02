# Draft PR 점검 결과 — 2026-08-25

| PR | 현재 상태 | 판정 | 다음 조치 |
|---|---|---|---|
| #2 초기 개발환경·비동기 파이프라인 통합 | Draft, CI 통과, 병합 가능, base `develop` | 병합 준비 | 기준 브랜치 변경 완료. 팀원 승인·통합 QA 후 병합 |
| #8 KLoSA·KNHANES 공통 전처리 | Draft, CI 통과, 병합 가능, base 통합 브랜치 | 보류 후 재검토 | #2 병합 후 `develop` 기준으로 갱신, 원본 데이터·산출물 제외와 누수 테스트 확인 |
| #4 Recall 중심 모델·앙상블 | Draft, 충돌, 상태 검사 없음 | 수정 필요 | #2·#8 반영 뒤 재배치하고 공통 분할·임계값 선택·재현 명령·artifact 계약 보완 |
| #11 균형 상위 5개 모델 재현 기준 | Draft, CI 통과, 병합 가능, base 통합 브랜치 | 중복 검토·보류 | #4와 실험 코드·후보 모델 중복을 정리하고 하나의 후보/앙상블 흐름으로 통합 |

## 병합 순서

1. `#2 → develop`: 현재 실행 가능한 MVP와 통합 기반 반영
2. `#8 → develop`: 공통 전처리·데이터 계약 반영
3. `#4 또는 #11`: 공통 분할로 재현한 후보를 선택해 artifact 패키징
4. 기능 브랜치 → `develop`, 통합 검증 후 `develop → main` Release PR

## 검토 공통 기준

- 원본 KLoSA·KNHANES, 모델 바이너리, 개인정보, `.env`가 포함되지 않았는가
- 전처리 통계와 임계값이 Train/Validation 범위에서만 학습·선택되었는가
- 참여자 단위 Train·Validation·Test 분리가 재현되는가
- Recall만 높이고 Specificity가 무너진 모델을 채택하지 않았는가
- 8개 입력 특성·모델 버전·특성 스키마·임계값 버전이 웹 계약과 일치하는가
- 관련 테스트·README·실험 결과가 함께 갱신되었는가

## Issue 정리 기준

- #1 초기 환경: #2가 `develop`에 병합되고 실행 검증되면 종료
- #7 공통 전처리: #8 병합 후 완료 조건을 확인하고 종료
- #3 연령·베이스라인 모델: #4/#11 통합 모델 PR에서 해결 여부 확인
- #5, #9: 구현 PR은 통합 브랜치에 반영되었으므로 #2 병합 후 수용 기준을 확인하고 종료
- [#12 실제 모델 Artifact Provider·E2E 통합](https://github.com/AI-HealthCare-05/AH_05_02/issues/12): Sprint 3 모델 연동 완료 조건 추적
